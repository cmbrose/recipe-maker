<% content_for :title, recipe.name %>

<div class="container">
  <div class="row">
    <div class="col-sm-12">
      <%= react_component("recipes/RecipeEditor", { 
        recipe: recipe, 
        update_url: recipe_path(recipe), 
        delete_url: recipe_path(recipe), 
        show_url: recipe_path(recipe),
        default_preview: image_path("unknown.jpg")
      }) %>
    </div>
  </div>
</div>